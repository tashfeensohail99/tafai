import { ClientMessagesPage } from '@/components/portal/ClientMessagesPage';

export const metadata = { title: 'Messages — Client Portal' };

export default function MessagesPage() {
  return <ClientMessagesPage />;
}
