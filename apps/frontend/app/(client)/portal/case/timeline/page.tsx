import { ClientTimelinePage } from '@/components/portal/ClientTimelinePage';

export const metadata = { title: 'Timeline — Client Portal' };

export default function TimelinePage() {
  return <ClientTimelinePage />;
}
